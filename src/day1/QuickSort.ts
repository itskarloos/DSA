function partition(numbers: number[], lo: number, hi: number): number {
  let pivot = numbers[hi];
  let idx = lo - 1;

  for (let i = 0; i < numbers[hi]; ++i) {
    if(pivot< numbers[i])
  }



}
export default function quick_sort(arr: number[]): void {

}
